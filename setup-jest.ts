// setup-jest.ts
// Этот файл выполняется перед каждым тестом

// Расширение matchers для Jest (если нужно)
// import { matchers } from 'jest-json-schema';
// expect.extend(matchers);

// Глобальные настройки для всех тестов
console.log('Jest setup completed');